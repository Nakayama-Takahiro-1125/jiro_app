<div class="top-hero-wrapper text-center">
  <div class="container top-hero-container">
    <h1 class="text-red">Jiro app</h1>
    <h2 class="text-red">ジロリアンの為のSNSサービスです</h2>
    <div class="row">
      <% if user_signed_in? %>
        <div class="col-md-offset-3 col-md-6">
          <%= link_to 'ログアウト', destroy_user_session_path, class: 'btn btn-block btn-white', method: :delete %>
      <% else %>
        <div class="col-md-offset-3 col-md-3">
          <%= link_to '新規登録', new_user_registration_path, class: 'btn btn-block btn-white' %>
        </div>
        <div class="col-md-3">
          <%= link_to 'ログイン', new_user_session_path,class: 'btn btn-block btn-white' %>
        </div>
      <% end %>
    </div>
  </div>
</div>